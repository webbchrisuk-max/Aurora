name: Aurora Monzo – Hourly Pot + Transaction Sync

on:
  schedule:
    - cron: "0 * * * *"   # every hour UTC
  workflow_dispatch:

concurrency:
  group: aurora-monzo-hourly
  cancel-in-progress: false

jobs:
  sync_monzo_hourly:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      # =====================================================
      # STEP 1 – Fetch Transfers sheet
      # =====================================================
      - name: 🟢 Fetch Transfers sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/Transfers" -o transfers.json

      # =====================================================
      # STEP 2 – Fetch Transactions sheet
      # =====================================================
      - name: 🟢 Fetch Transactions sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/Transactions" -o transactions.json

      # =====================================================
      # STEP 3 – Fetch Direct Debits sheet
      # =====================================================
      - name: 🟢 Fetch DirectDebits sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/DirectDebits" -o directdebits.json

      # =====================================================
      # STEP 4 – Ensure state tracking
      # =====================================================
      - name: 📦 Ensure state folder
        run: |
          mkdir -p .aurora
          if [ ! -f .aurora/monzo_transfers_applied.json ]; then
            echo "[]" > .aurora/monzo_transfers_applied.json
          fi

      # =====================================================
      # STEP 5 – Apply NEW Transfers
      # =====================================================
      - name: 🔄 Apply NEW Transfers
        run: |
          node <<'EOF'
          const fs = require('fs');

          const transfers = JSON.parse(fs.readFileSync('transfers.json'));
          const applied = JSON.parse(fs.readFileSync('.aurora/monzo_transfers_applied.json'));

          const newTransfers = transfers.filter(t => !applied.includes(t.UID));

          if (newTransfers.length > 0) {
            newTransfers.forEach(t => applied.push(t.UID));

            fs.writeFileSync('.aurora/monzo_transfers_applied.json', JSON.stringify(applied, null, 2));
          }
          EOF

      # =====================================================
      # STEP 6 – Auto-tick Paid Direct Debits
      # =====================================================
      - name: 🔘 Auto-mark Paid Direct Debits
        run: |
          node <<'EOF'
          const fs = require('fs');

          // Load Direct Debits sheet
          const ddSheet = JSON.parse(fs.readFileSync('directdebits.json', 'utf8'));

          // Load Monzo transactions
          const transactions = JSON.parse(fs.readFileSync('transactions.json', 'utf8'));

          // Today’s date (normalised)
          const today = new Date();
          today.setHours(0,0,0,0);

          let updated = false;

          ddSheet.forEach(dd => {
            const ddName = (dd["Name"] || "").toLowerCase().trim();
            const ddDate = new Date(dd["Date"]);
            const ddPaid = dd["Paid"] === true;

            ddDate.setHours(0,0,0,0);

            // Already paid? skip
            if (ddPaid) return;

            // Only match DDs due today or earlier
            if (ddDate > today) return;

            // Try to match with Monzo transaction
            const match = transactions.find(tx => {
              const desc = (tx.description || "").toLowerCase();
              const merch = (tx.merchant || "").toLowerCase();
              return desc.includes(ddName) || merch.includes(ddName);
            });

            if (match) {
              dd["Paid"] = true;
              updated = true;
            }
          });

          // Save back if updated
          if (updated) {
            fs.writeFileSync('directdebits.json', JSON.stringify(ddSheet, null, 2));
          }
          EOF

      # =====================================================
      # STEP 7 – Commit changes back to GitHub
      # =====================================================
      - name: 📤 Commit and Push
        run: |
          git config user.name "Aurora Automation"
          git config user.email "actions@github.com"

          git add .
          git commit -m "Aurora Monzo: Hourly Sync + Auto-tick DirectDebits" || echo "No changes to commit"
          git push || echo "No changes to push"
