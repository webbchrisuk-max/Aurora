name: Aurora Monzo – Hourly Pot + Transaction Sync

on:
  schedule:
    - cron: "0 * * * *"   # every hour UTC
  workflow_dispatch:

concurrency:
  group: aurora-monzo-hourly
  cancel-in-progress: false

jobs:
  sync_monzo_hourly:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      # =====================================================
      # STEP 1 – Fetch MonzoCore sheet (MAIN ACCOUNT + POTS)
      # =====================================================
      - name: 🟢 Fetch MonzoCore sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/MonzoCore" -o monzocore.json

      # =====================================================
      # STEP 2 – Fetch Transfers sheet
      # =====================================================
      - name: 🟢 Fetch Transfers sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/Transfers" -o transfers.json

      # =====================================================
      # STEP 3 – Fetch Transactions sheet
      # =====================================================
      - name: 🟢 Fetch Transactions sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/Transactions" -o transactions.json

      # =====================================================
      # STEP 4 – Fetch DirectDebits sheet
      # =====================================================
      - name: 🟢 Fetch Direct Debits sheet
        run: |
          curl -s "https://opensheet.elk.sh/10MdgQKc4tParno7pNkz40eBGz308wxHu1u3gvJe_WsE/DirectDebits" -o directdebits.json

      # =====================================================
      # STEP 5 – Ensure state folder
      # =====================================================
      - name: 📦 Ensure state folder
        run: |
          mkdir -p .aurora
          if [ ! -f .aurora/monzo_transfers_applied.json ]; then
            echo "[]" > .aurora/monzo_transfers_applied.json
          fi

      # =====================================================
      # STEP 6 – Apply NEW Transfers (UID filtering)
      # =====================================================
      - name: 🔄 Apply NEW Transfers
        run: |
          node <<'EOF'
          const fs = require('fs');

          const transfers = JSON.parse(fs.readFileSync('transfers.json', 'utf8'));
          const applied = JSON.parse(fs.readFileSync('.aurora/monzo_transfers_applied.json', 'utf8'));

          const newTransfers = transfers.filter(t => t.UID && !applied.includes(t.UID));

          if (newTransfers.length > 0) {
            newTransfers.forEach(t => applied.push(t.UID));
            fs.writeFileSync('.aurora/monzo_transfers_applied.json', JSON.stringify(applied, null, 2));
          }
          EOF

      # =====================================================
      # STEP 7 – Auto-tick Paid Direct Debits
      # =====================================================
      - name: 🔘 Auto-mark Paid Direct Debits
        run: |
          node <<'EOF'
          const fs = require('fs');

          const ddSheet = JSON.parse(fs.readFileSync('directdebits.json', 'utf8'));
          const transactions = JSON.parse(fs.readFileSync('transactions.json', 'utf8'));

          const today = new Date();
          today.setHours(0,0,0,0);

          let updated = false;

          ddSheet.forEach(dd => {
            const ddName = (dd["Name"] || "").toLowerCase().trim();
            const ddDate = new Date(dd["Date"]);
            const ddPaid = dd["Paid"] === true;

            ddDate.setHours(0,0,0,0);

            if (ddPaid) return;
            if (ddDate > today) return;

            const match = transactions.find(tx => {
              const merged = ((tx.Merchant || "") + " " + (tx.Description || "")).toLowerCase();
              return merged.includes(ddName);
            });

            if (match) {
              dd["Paid"] = true;
              updated = true;
            }
          });

          if (updated) {
            fs.writeFileSync('directdebits.json', JSON.stringify(ddSheet, null, 2));
          }
          EOF

      # =====================================================
      # STEP 8 – UPDATE AuroraMonzo.json (Structured Output)
      # =====================================================
      - name: 🧿 Update AuroraMonzo.json with latest data
        run: |
          node <<'EOF'
          const fs = require('fs');

          // Load existing OR create empty object
          let core = {};
          if (fs.existsSync('AuroraMonzo.json')) {
            core = JSON.parse(fs.readFileSync('AuroraMonzo.json', 'utf8'));
          }

          // Load the fetched files
          const monzoCoreRaw = JSON.parse(fs.readFileSync('monzocore.json', 'utf8'));
          const transactions = JSON.parse(fs.readFileSync('transactions.json', 'utf8'));
          const direct = JSON.parse(fs.readFileSync('directdebits.json', 'utf8'));
          const transfers = JSON.parse(fs.readFileSync('transfers.json', 'utf8'));
          const applied = JSON.parse(fs.readFileSync('.aurora/monzo_transfers_applied.json', 'utf8'));

          // Convert MonzoCore sheet into structured object
          const accountCore = {
            main_balance: parseFloat(monzoCoreRaw[0]["Main Balance"] || 0),
            payday_last: monzoCoreRaw[0]["Payday Last"] || "",
            payday_next: monzoCoreRaw[0]["Payday Next"] || "",
            income_received: parseFloat(monzoCoreRaw[0]["Income Received"] || 0),
            pots: {}
          };

          // Build pots dictionary
          monzoCoreRaw.forEach(row => {
            if (row["Pot Name"] && row["Pot Balance"]) {
              accountCore.pots[row["Pot Name"]] = parseFloat(row["Pot Balance"]);
            }
          });

          // Write into final JSON
          core["monzo"] = {
            account_core: accountCore,
            transactions: transactions,
            direct_debits: direct,
            transfers: transfers,
            state: applied
          };

          // Pretty print each file
          fs.writeFileSync('monzocore.json', JSON.stringify(monzoCoreRaw, null, 2));
          fs.writeFileSync('transactions.json', JSON.stringify(transactions, null, 2));
          fs.writeFileSync('directdebits.json', JSON.stringify(direct, null, 2));
          fs.writeFileSync('transfers.json', JSON.stringify(transfers, null, 2));
          fs.writeFileSync('.aurora/monzo_transfers_applied.json', JSON.stringify(applied, null, 2));

          // Write master file
          fs.writeFileSync('AuroraMonzo.json', JSON.stringify(core, null, 2));
          EOF

      # =====================================================
      # STEP 9 – COMMIT & PUSH
      # =====================================================
      - name: 📤 Commit and Push
        run: |
          git config user.name "Aurora Automation"
          git config user.email "actions@github.com"

          git add .
          git commit -m "Aurora Monzo: Hourly Sync (MonzoCore + Pots + Balance Update)" || echo "No changes to commit"
          git push || echo "No changes to push"